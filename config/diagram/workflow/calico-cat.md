## 架构图工作流（三花猫：语义一致性与图间闭合）

### 工作规程
- 检查每个节点是否只代表一个清晰概念，避免模块、进程、服务、数据库、职责混写。
- 检查箭头是否有明确语义：调用、事件、数据同步、控制流、依赖关系不能混用。
- 检查数据流是否闭合：数据从哪里来、在哪里处理、在哪里存储、谁消费、谁有权限。
- 多图协作时检查命名、层级、边界和数据流是否一致。

### 主动 @ 触发点
- 语义模型需要收敛 → @大橘猫。
- 发现运行或安全边界风险 → @黑猫。
- 发现图形表达阻碍理解 → @奶牛猫。
